# Core numerics
numpy>=1.23
pandas>=1.5

# Machine-learning & evaluation
scikit-learn>=1.3

# Deep-learning
torch>=2.0                 # CPU/GPU build auto-resolved by pip / Kaggle

# Visualisation (only used in module self-tests / EDA notebooks)
matplotlib>=3.7

# Quality-of-life utilities
tqdm>=4.66                 # progress bars for training loops
pyyaml>=6.0                # load configs/config.yaml

# (Leave blank line at end of file)
